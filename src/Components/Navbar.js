const Navbar = () => {
    
    return (
        <div>
            <nav className="navbar">
                <h2 className="navbar-para">Top Courses</h2>
            </nav>
        </div>
    );
}

export default Navbar;